(() => { const buttons=[...document.querySelectorAll('[data-stock-tab]')]; const panels=[...document.querySelectorAll('[data-panel]')]; function open(name){buttons.forEach(b=>b.classList.toggle('active',b.dataset.stockTab===name));panels.forEach(p=>p.hidden=p.dataset.panel!==name);localStorage.setItem('stockTab',name);} buttons.forEach(b=>b.addEventListener('click',()=>open(b.dataset.stockTab))); open(localStorage.getItem('stockTab')||'produtos'); })();
